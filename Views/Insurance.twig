<h1 class="app-main-title">{{ translate('Insurance') }}</h1>

<p class="mb-4">This app covers the core insurance process areas: policies, claims, and renewals.</p>

<hblreact-insurance-table-policies
  string:tag="table-policies"
  int:record-id="{{ viewParams.recordId }}"
  string:view="{{ viewParams.view }}"
  json:filters='{{ viewParams.filters|json_encode }}'
></hblreact-insurance-table-policies>
